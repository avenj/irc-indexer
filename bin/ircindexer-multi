#!perl

use strict;
use warnings;

use File::Find;
use File::Path;

use YAML::XS;

use POE:

use IRC::Indexer::Trawl::Multi;

use IRC::Indexer::Output::JSON;
use IRC::Indexer::Output::YAML;

use Getopt::Long;
my $opts = {
  version => sub {
  
    exit 0
  },
  
  help => sub {
  
    exit 0
  },
};

GetOptions( $opts,
  qw/
    help
    version
    
    verbose:0
    
    config=s
    
    exampleconf=s
    
    
  /;
);

my $cfg = {};

## FIXME use IRC::Indexer::Conf

sub _start {

  ## FIXME
  ## Spawn pool of trawlers
  ## Feed successful trawlers to Info::Network
  ## When everyone is done, call write-out
}

