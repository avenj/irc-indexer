package IRC::Indexer::Collector;

# FIXME
#   Collect results from dispatcher(s) via SUB
#   Allow clients to retrieve via ROUTER
#   Store to configurable DB handle, default to SQLite?
#   Impl basic schema (from sharedir?)
#
#   Impl basic searching via strings or glob-y patterns
#     (Should be able to create portable-ish SQL from same?)

1;
